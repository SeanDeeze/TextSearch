version: "3.8"
services:
    app-backend:
        build: ./server/
        image: app_backend
        container_name: backend
        restart: always
        ports:
            - 8082:8082app-frontend:
        build: ./GUI/
        image: macroplan
        container_name: frontend
        restart: always
        ports:
            - 4200:4200
        expose:
            - "4200"